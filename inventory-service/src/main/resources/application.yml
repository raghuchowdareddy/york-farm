server:
  port: ${PORT:${SERVER_PORT:46457}}
---
spring:
  application:
    name: inventory-service
  datasource:
    url: jdbc:hsqldb:hsql://localhost
    initialize: true
  profiles:
    active: development
---
spring:
  application:
    name: inventory-service
  datasource:
    url: jdbc:mysql://localhost:3306/farm
    initialize: true
    username: root
    password: root
  profiles:
    active: production
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: false
    hibernate:
      ddl-auto: update